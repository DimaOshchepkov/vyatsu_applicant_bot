name: Run tests

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [3.8, 3.9, 3.10]

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install uv
        run: |
          python -m pip install --upgrade pip
          pip install uv

      - name: Install dependencies
        run: |
          uv pip install -r requirements-test.txt

      - name: Run tests
        run: |
          pytest --maxfail=1 --disable-warnings -q

# jobs:
#   test:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4
        
#       - name: Set up Docker
#         uses: docker/setup-buildx-action@v3

#       - name: Create dummy .env file
#         run: echo "" > .env

#       - name: Create dummy .env.vector_db file
#         run: echo "" > .env.vector_db
       
#       - name: Build test-runner with cache
#         uses: docker/build-push-action@v6
#         with:
#           context: .                       
#           file: ./Dockerfile              
#           build-args: |
#             REQUIREMENTS=requirements-test.txt  
#           tags: vyatsu_applicant_bot-test-runner:latest
#           load: true
#           cache-from: type=gha
#           cache-to: type=gha,mode=max

#       - name: Run tests
#         run: |
#           docker compose -f docker-compose.yml up --abort-on-container-exit --exit-code-from test-runner --no-build test-runner test-db
   
      
#       - name: Show logs if failed
#         if: failure()
#         run: |
#           echo "===== test-runner logs ====="
#           docker compose logs test-runner
          